{
  "name": "@homebase-id/web-drop-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "esbuild src/crypto.ts --format=esm --outfile=test/_build/crypto.mjs --log-level=silent && node --test 'test/*.test.mjs'"
  }
}
