export const DEFAULT_PAYLOAD_KEY = 'dflt_key';
export const MAX_HEADER_CONTENT_BYTES = 5000; // 10240 bytes is the server limit for the total Local App Content; We are using 4000 bytes for the header content to allow the extra data stored in the header
